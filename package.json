{
  "name": "omp-mailbox-plugin",
  "version": "0.1.0",
  "type": "module",
  "files": ["src"],
  "omp": {
    "extensions": ["./src/index.ts"]
  },
  "description": "Thin OMP notification adapter for mailbox protocol (requires canonical mailbox CLI via codeagent-py)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "^17.2.3",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
